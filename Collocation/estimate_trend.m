function [V_trend_stack, V_sigma_trend_stack] = estimate_trend(lat, long, LatGrid, LongGrid, V_trend_grid)

% do bilinear interpolation for station location points

for i = 1:length(lat)
    
    
    
end


end